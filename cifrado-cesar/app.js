function cipher(message){
 var openMessage
  if (message = )

}
 function decipher(){
  var newMessage = "";
   for (i = 0; i < newMessage.length; i++){
    var otherMsn = message.charCodeAt(i);
   if (65 <= otherMsn && otherMsn <= 122);
    newMessage += string.fromCharCode((otherMsn - 65 + toMessage) % 26 + 65).charCodeAt(0);
  }
 return newMessage;

}


var message = prompt("Ingrese su mensaje");
